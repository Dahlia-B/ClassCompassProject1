<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="ClassCompassApp.TestPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Test Page">
    <ScrollView>
        <VerticalStackLayout Spacing="25" Padding="30,0" VerticalOptions="Center">
            <Label 
                x:Name="TestLabel"
                Text="App is running successfully!"
                FontSize="18"
                HorizontalOptions="Center" />
            
            <!-- Comment out Button initially to test if that's the issue -->
            <!--
            <Button 
                x:Name="TestButton"
                Text="Test Button"
                Clicked="OnTestButtonClicked"
                HorizontalOptions="Center" />
            -->
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
